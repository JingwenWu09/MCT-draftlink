#include <math.h>
#include <stdio.h>
#include <stdlib.h>

int main(int argc, char **argv) {
  int *ptr = 0;

  for (unsigned i = 0; i < 2; i++) {
    switch (argc) {
      int a;
    default:
      ptr = &a;
      *ptr = 12345;
      break;
    }
  }

  return 0;
}
