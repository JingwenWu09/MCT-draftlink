#include <math.h>
#include <stdio.h>
#include <stdlib.h>

int a, b;

int main(void) {
  int temp;
  int *c = &temp;
  unsigned long d = 0;
  while (1) {
    switch (b)
    case 'S':
      d = a;
    c = __builtin_malloc(d);
  }

  return 0;
}
