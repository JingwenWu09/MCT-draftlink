#include <math.h>
#include <stdio.h>
#include <stdlib.h>

int a, b = -2;
int main() {
  int d = 0;
  int t;
  
  if (b || t) {
    if (!a) {
      d = b;
    }
    while (d > -1) {
      ;
    }
  }
  return 0;
}
