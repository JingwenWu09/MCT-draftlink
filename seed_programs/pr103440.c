#include <math.h>
#include <stdio.h>
#include <stdlib.h>

int a, b, c, d, e;
int main() {
  int f = 2, g = 1, h = -3;
	do{
		c = b ^ 1;
		if (!f) {
		  while (d == g) {
				if (g) {
					a = 0;
				}
			}
			return 0;
		}
		if (d) {
		  g = e;
		}
		f = h;
  } while (!c) ;

  if (g) {
    a = 0;
  }

  while (d == g) {
    if (g) {
    	a = 0;
  	}
  }
  return 0;
}
