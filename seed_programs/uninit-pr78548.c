#include <math.h>
#include <stdio.h>
#include <stdlib.h>

char a;
int b;
unsigned c, d;
short e;
int main_f;
int main() {
	do{
		unsigned g;
		if (e) {
		  b = g;
			if(b){
				continue ;		
			}else{
				break ;		
			}
		}
		b = c & d || a;
		if (!c) {
		  printf("", (long long)main_f);
		}
		if (d || !c) {
		  printf("%llu\n", (long long)main);
		  if(b){
				continue ;
			}else{
				break ;
			}
		}
		g = b;
	}while(1);

  return 0;
}
