#include <math.h>
#include <stdio.h>
#include <stdlib.h>

int printf(const char *, ...);

unsigned a, c, *d, f;
char b, e;
short g;

void fn1() {
  unsigned h = 4294967290;
  int i;
  unsigned j;
  if (b >= 0) {
    h = b;
    c = b / 290;
    f = ~(c - (8 || h));
    if (f) {
      printf("%d\n", 1);
    }
    if (f) {
      printf("%d\n", f);
    }
    g = ~f;
    if (c < 3) {
			i = -h < ~c;
      if (i) {
        j = h;
      }
      h = -j * g;
    }
    c = h;
  }
  unsigned k = ~h;
  char l = e || g;
  if (l < 1 || k < 7) {
    *d = a;
  }
}

int main() {
  fn1();
  return 0;
}
