#include <math.h>
#include <stdio.h>
#include <stdlib.h>

int printf(const char *, ...);
char a = 139, b;
int main() {
  char c = 173;
  b = a;
  while (c <= a || a < -117) {
    c = printf("0\n");
  }
  return 0;
}
