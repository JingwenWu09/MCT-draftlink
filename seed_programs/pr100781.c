#include <math.h>
#include <stdio.h>
#include <stdlib.h>

struct a {
  int b;
};
long c(short d, long e, struct a f) {
	do{
		int h = f.b <= e, i = d, n = h >= d;
		if (n) {
		  continue ;
		}
		long l = 5;
		if(!l){
			d = 0;
		}
		if (d) {
		  return f.b;
		}
  } while(1);
}
int main() {
}
